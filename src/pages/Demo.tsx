import React from 'react';
import { Card, Typography } from 'antd';

export default (): React.ReactNode => {
  return (
    <Card title="演示">
      <Typography>如果不去做的话，那就意味着不重要，至少现目前来看。</Typography>
    </Card>
  );
};
